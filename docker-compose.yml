services:
  vboxstock:
    image: ghcr.io/mfwadejr/vboxstock:latest
    container_name: vboxstock
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - TZ=America/New_York
